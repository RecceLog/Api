services:
  reccelogapi:
    image: reccelogapi:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - reccelog-shared-network

networks:
  reccelog-shared-network:
    external: true